pub fn generate_schedules(
    classes: &[Class],
    subject_codes: &[&str],
    periods: &[Period],
) -> HashSet<Vec<Class>> {
    let mut schedules: HashSet<Vec<Class>> = HashSet::new();
    schedules
}
